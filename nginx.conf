# Nginx configuration for EMDR app
# Place this in /etc/nginx/sites-available/remote-emdr.nl

# Rate limiting zones for DDoS protection
# More lenient for polling-based app (2 API calls every 500ms = 4 req/s minimum needed)
limit_req_zone $binary_remote_addr zone=api_limit:10m rate=20r/s;
limit_req_zone $binary_remote_addr zone=general_limit:10m rate=50r/s;
limit_conn_zone $binary_remote_addr zone=conn_limit:10m;

# Upstream with keepalive for better performance
upstream flask_backend {
    server 127.0.0.1:5000;
    keepalive 32;
}

# Redirect HTTP to HTTPS
server {
    listen 80;
    listen [::]:80;
    server_name remote-emdr.nl www.remote-emdr.nl;
    
    # Let's Encrypt certificate verification
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }
    
    # Redirect all other traffic to HTTPS
    location / {
        return 301 https://$server_name$request_uri;
    }
}

# HTTPS Server
server {
    listen 443 ssl;
    listen [::]:443 ssl;
    http2 on;
    server_name remote-emdr.nl www.remote-emdr.nl;

    # SSL Configuration
    ssl_certificate /etc/letsencrypt/live/remote-emdr.nl/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/remote-emdr.nl/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 10m;
    
    # HSTS (HTTP Strict Transport Security)
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
    
    # Keep connections alive longer for polling
    keepalive_timeout 300s;
    keepalive_requests 1000;
    
    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    
    # Connection limits (max 10 connections per IP)
    limit_conn conn_limit 10;
    
    # General rate limiting (30 requests per second)
    limit_req zone=general_limit burst=50 nodelay;
    
    # Root directory for static files
    root /var/www/remote-emdr/frontend/dist;
    index index.html;
    
    # Gzip compression
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css text/xml text/javascript application/javascript application/xml+rss application/json;
    
    # API endpoints - proxy to Flask backend
    location /api/ {
        # More lenient rate limiting for API (allow polling every 500ms = 2 req/s per connection)
        # With burst to handle multiple simultaneous sessions
        limit_req zone=api_limit burst=50 nodelay;
        
        proxy_pass http://flask_backend;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection '';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        
        # Keep connections alive for polling
        proxy_buffering off;
        proxy_request_buffering off;
        
        # Extended timeouts for long-lived connections
        proxy_connect_timeout 300s;
        proxy_send_timeout 300s;
        proxy_read_timeout 300s;
        
        # Enable keepalive
        proxy_set_header Connection "keep-alive";
    }
    
    # Static files with caching
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
    
    # Vue.js routing - return index.html for all non-file requests
    location / {
        try_files $uri $uri/ /index.html;
    }
    
    # Deny access to hidden files
    location ~ /\. {
        deny all;
        access_log off;
        log_not_found off;
    }
    
    # Block common exploit attempts
    location ~* (\/wp-admin|\/wp-login|\.php$) {
        deny all;
        return 404;
    }
    
    # Logging
    access_log /var/log/nginx/remote-emdr-access.log;
    error_log /var/log/nginx/remote-emdr-error.log;
}
